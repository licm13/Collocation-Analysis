[pytest]
# Only collect tests from the top-level 'tests' directory to avoid
# third-party/legacy test scripts included in the repository (e.g., ETCC/).
testpaths = tests
python_files = test_*.py
filterwarnings =
    default